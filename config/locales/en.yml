en:
  activerecord:
    errors:
      messages:
        invalid_date: 'is invalid'
      models:
        reservation:
          attributes:
            end_date:
              less_than_date: 'greater than %{field}'
